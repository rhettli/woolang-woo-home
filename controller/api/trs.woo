local class_translate = {}

function class_translate:new(o)
    o = o or {}
    --setmetatable(o, self)
    --self.__index = self

    self.version = "1"

    return self
end

function class_translate:search()


end

return _extend(class_translate, "controller.api.base")
