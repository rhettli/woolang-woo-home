-- 太重了，废弃，请在各个controller中使用_base.woo替换

return {
    handler = function(file,method)
        -- out('access file with method')


        --return false  -- false will end process [返回false，停止程序执行]
        return true
    end
}